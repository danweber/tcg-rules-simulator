
export class EffectTree_UNUSED_OH_NO {
    depth: number; // redundant
    label: string;
    fulltext: string;
    n_player: number;

    constructor(depth: number, label: string, fulltext: string, n_player: number) {
        this.depth = depth;
        this.label = label;
        this.fulltext = fulltext;
        this.n_player = n_player;
    }
}
